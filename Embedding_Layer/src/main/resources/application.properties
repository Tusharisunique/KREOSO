# Embedding Layer runs on port 8082
server.port=8082

# PostgreSQL connection — Supabase
spring.datasource.url=jdbc:postgresql://db.qcnhzndyptepoadjaldd.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=dsj3mjhncWPBf9s7
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings (important for Supabase cloud connections)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Ollama base URL (llama3.1:8b for chat, nomic-embed-text for embeddings)
ollama.base-url=http://localhost:11434
